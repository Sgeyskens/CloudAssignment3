# Start from the official MySQL base image
FROM mysql:8.0

# Set environment variables (can also be set in docker-compose or runtime)
ENV MYSQL_ROOT_PASSWORD=flaskcrud
ENV MYSQL_DATABASE=flaskcrud
ENV MYSQL_USER=flaskcrud
ENV MYSQL_PASSWORD=flaskcrud

# Expose MySQL port
EXPOSE 3306
